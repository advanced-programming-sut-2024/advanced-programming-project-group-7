package model;

public class Mardroeme extends Card{
    public Mardroeme(String cardName, int countOfCard, boolean isSpecial, int power) {
        super(cardName, countOfCard, isSpecial, power);
    }
}
