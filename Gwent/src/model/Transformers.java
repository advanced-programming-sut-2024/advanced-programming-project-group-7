package model;

public class Transformers extends Card{
    public Transformers(String cardName, int countOfCard, boolean isSpecial, int power) {
        super(cardName, countOfCard, isSpecial, power);
    }
}
