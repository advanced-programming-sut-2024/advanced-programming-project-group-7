package model;

public class TightBond extends Card{
    public TightBond(String cardName, int countOfCard, boolean isSpecial, int power) {
        super(cardName, countOfCard, isSpecial, power);
    }
}
