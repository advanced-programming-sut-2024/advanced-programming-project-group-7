package model;

public class CommandersHorn extends Card {

    public CommandersHorn(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
