package model.cards;

import model.Card;

public class Medic extends Card {

    public Medic(String cardName, int countOfCard, boolean isSpecial, int power, String factionName, int rows, boolean isHero) {
        super(cardName, countOfCard, isSpecial, power, factionName, rows, isHero);
    }
}
