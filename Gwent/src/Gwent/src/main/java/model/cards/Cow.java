package model.cards;

import model.Card;

public class Cow extends Card {
    public Cow(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
