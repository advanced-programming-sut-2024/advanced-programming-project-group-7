package model.cards;

import model.Card;

public class TightBond extends Card {
    public TightBond(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
