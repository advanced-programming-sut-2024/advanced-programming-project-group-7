package model.cards;

import model.Card;

public class Horn extends Card {
    public Horn(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
