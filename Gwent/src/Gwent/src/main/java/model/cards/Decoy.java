package model.cards;

import model.Card;

public class Decoy extends Card {

    public Decoy(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
