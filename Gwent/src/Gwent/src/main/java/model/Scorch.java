package model;

public class Scorch extends Card{

    public Scorch(String cardName, int countOfCard, boolean isSpecial, int power, String factionName) {
        super(cardName, countOfCard, isSpecial, power, factionName);
    }
}
